# Auto-provision the Prometheus datasource so the dashboard works on first boot.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    uid: justrebootit-prometheus
    jsonData:
      timeInterval: 10s
